import React from 'react';
import CardHeader from '../../../general/card/CardHeader';

export default function AnimalHeader({ title, image }) {
    return <CardHeader title={title} image={image} />;
}
